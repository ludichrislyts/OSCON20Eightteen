/* globals: expect */

import sessions from './sessions.mjs';

describe('sessions', () => {
  describe('new()', () => {
    it('should be awesome', () => {
      expect(true).toEqual(true);
    });
  });
});
