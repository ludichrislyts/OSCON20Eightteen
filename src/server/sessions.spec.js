/* globals: expect */

import sessions from './sessions.js';

describe('sessions', () => {
  describe('new()', () => {
    it('should be awesome', () => {
      expect(true).toEqual(true);
    });
  });
});
